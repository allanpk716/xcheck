# xcheck

用本地多个异构 AI agent CLI 盲评同一个技术问题/方案,产出**已验证的三分类问题清单**;人只在停点拍板。本文件是术语表,不是规格。

## Language

### 核心模型

**三分类(triage)**:
把每条外部 agent 反馈按**可验证性**归入①②③三类。分类对象是"这条反馈能不能被验证",不是"严不严重"。
_Avoid_: 优先级、严重度分级

**第①类 · 可直接证实**:
查代码 / 配置 / 文档就能判真伪的反馈。验证手段是只读查证。

**第②类 · 可实验验证**:
需要跑一个沙箱实验才能判定的反馈。验证手段是实验(禁改业务代码、禁联网)。

**第③类 · 存疑**:
无法验证、只能参考的反馈。不做验证,原样保留并标注。

**已验证三分类清单**:
xcheck 的**最终交付物**:每条①带证实/证伪/查无实据结论,每条②带实验结果,③原样带 ⚠️。用户在它面前做最终决策。
_Avoid_: 必改清单(旧设想"用户决策后才产清单",未实现;现行术语见「必改项」)

**必改项(must-fix)**:
①证实 + ②成立的机械并集,每条带编号、来源、严重度与证据。"必改"是管道判定,不是命令——用户在停点有完全否决权。
_Avoid_: 必改清单、遗留问题清单

**结论区(verdict block)**:
已验证三分类清单置顶的纯机械拼装块:状态行、必改项全文、各家裁决、信号、统计。每个字段都有既定来源,零综合判断——裁决与必改项"对不上"是留给用户看的信号,系统不圆场。

**状态(status)**:
单个评审环的推进判定,两值:✅ 可推进(必改项空)/ ⚠️ 先修订再推进(必改项非空)。与「终态」不同级:状态管一轮,终态管整条链。
_Avoid_: 与终态混用

**评审附录(review appendix)**:
链达成结论性终态时,追加到**被评文档文末**的一节:结论、必改、查过不用理、开发时盯住(触发点 + 命中动作)。定位是**评审参考,以实际执行为准**;但"撞上关注项停下反馈"是动作,不是参考。原稿正文永不动,文末附录是唯一例外(按 ts 幂等重写)。
_Avoid_: watchlist 独立文件、项目级汇总索引(开发的人读的是计划/方案本身)

### 自动链

**自动链**:
一次 `/xcheck` 触发后、停点之前的全自动推进路径:盲评 → 三分类 → 查证① → 实验② → 交付。中间不需要人。
_Avoid_: 全流程(分不清含不含闭环旧义)

**停点(gate)**:
自动链上**唯一**等人拍板的时刻——呈现已验证三分类清单,用户决策。人的控制权在结果上,不在过程里。

**进度文件**:
自动链每个阶段完成即落盘的状态记录,是恢复与排障的**唯一权威**。"进行到哪了"只看盘,不依赖任何 agent 的会话记忆。
_Avoid_: 会话状态、内存进度

**断点续跑**:
触发时若检测到未完成的链,**自动发现、人工确认**("续旧的还是开新的?");续跑读进度文件,已完成阶段跳过,从第一个未完成阶段继续;阶段内做到一半崩了,该阶段整体重做。

**终态(terminal state)**:
自动链的六种合法结束:收敛 / 推倒重来 / 无需修订 / 用户不修 / 用户中止 / 完成(diag)。无需修订 = round 0 必改项空(含 ①+② 真空,或剩余条目全被证伪/实验不成立);用户不修 = 必改项非空但用户在停点拒绝修订。未达终态的目录视为"未完成",可续跑。

### 流程各步

**摄入(intake)**:
把不自包含的指代性输入("评审刚才讨论的方案")固化成中性、自包含的 proposal。
_Avoid_: 预处理、上下文提取

**冒烟(smoke)**:
fan-out 前对每家 agent 的活性预检(读一个小文件并回显);欠费 / 未登录 / 读不了文件的家在派发前剔除。
_Avoid_: 健康检查

**选集(SELECTED)**:
本轮实际参评的 agent 名单。来源优先级:`--agents` 参数 > 默认集 > 报错要求先设默认集。
_Avoid_: 候选集(CANDIDATES 是筛选前的名单,不是选集)

**派发(fan-out)**:
把同一个评审任务并行发给选集里的每家 agent,各自独立、互不可见。

**搬运工(carrier)**:
包着一家外部 agent 的 subagent,职责只有"调脚本 → 等结束 → 读文件 → 忠实搬运"。严禁掺入判断。
_Avoid_: 子代理、评审员

**汇总(synthesis)**:
主会话(强模型)综合各家结构化结论:共识 / 分歧 / 存疑。只发生在主会话,不在 subagent。

**查证(verification)**:
对①类条目的只读核实,产出 ✅ 证实 / ❌ 证伪 / ❓ 查无实据,各附一行证据。
_Avoid_: 复核(和复审混淆)

**实验(experiment)**:
对②类条目的沙箱验证:临时文件落 `.xcheck/<ts>/exp/`,禁改业务代码、禁联网、禁部署。产出成立 / 不成立 / 无定论。
_Avoid_: 测试、探测

**修订(revision)**:
针对必改项写出的方案新版本,落 `<原名>.rev<m>.md`,**原稿正文永不动**(唯一例外:终态在被评文档文末追加「评审附录」)。
_Avoid_: 修改、补丁

**复审(re-review)**:
对修订版完整重跑一轮评审 + 三分类 + 验证。硬上限 2 轮,超限判"推倒重来"。

**收敛(converged)**:
最新一轮必改项清零(①+②真空,或剩余条目全部被证伪/实验不成立)的终态。

**推倒重来(start over)**:
2 轮修订后仍存在①+②类问题时的终态判定:疑方案根基缺陷,建议重新设计而非继续修补。

### 基础约束

**异构(heterogeneity)**:
选集必须含 ≥1 个非 claude agent,否则是 Claude 自己审自己,第二意见价值归零。

**盲评(blind)**:
各家 agent 独立进程、互不可见、不知道彼此结论,防串通。
