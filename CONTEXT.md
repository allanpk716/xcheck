# xcheck

用本地多个异构 AI agent CLI 盲评同一个技术问题/方案。review 交付可追溯的事实、阻断裁定与可推进范围;diag 交付诊断与三分类。本文件是术语表,不是规格。

## Language

> **版本边界**:0.19.0实施审核改进第一批,0.20.0实施第二批交互方式/执行终点解耦,0.21.0新增第三批delivery_schema=1夜链同分支交付。下列review现行术语仅用于 `review_schema = 2`;无该字段的旧review保留旧释义,不重解释历史“必改/收敛”。diag保持旧语义,始终不实施、不建worktree。第四、五批(配置分离、真正权限隔离)尚未实施。权威契约见 [review-contract.md](xcheck/lib/review-contract.md)与[night-delivery.md](xcheck/lib/night-delivery.md),完整目标见[设计](docs/superpowers/specs/2026-09-19-xcheck-review-convergence-and-portability-design.md)与[分批计划](docs/superpowers/plans/2026-09-19-xcheck-review-convergence-and-portability.md)。

### 核心模型

**讨论共识**:
用户与助手经讨论形成的已确认目标、决策和约束,不等同于完整实施规格。助手未获确认的提议不属于共识。
_Avoid_: 将全部聊天内容或助手总结视作用户已同意的方案

**共识快照**:
本次审核的对象与必要背景:proposal.md 固定对象,context.md 保存必要原话,decisions.md 用稳定 D 编号区分已确认、未决、否决、假设和已替代决策。短答绑定原问题/选项,修正保留替代关系,缺失上下文如实标记。
_Avoid_: 实施规格、只摘用户短答的原话清单

**三分类(triage)**:
把反馈按**可验证性**归入①②③。review 的三类都有稳定 F 编号,事实与处置另行裁定;diag 沿用旧三分类。
_Avoid_: 优先级、严重度分级、阻断等级

**第①类 · 可直接证实**:
查代码 / 配置 / 文档就能判真伪的反馈。验证手段是只读查证,并不预先保证证实。

**第②类 · 可实验验证**:
需要跑本地沙箱实验才能判定的反馈。实验禁改业务代码、禁联网、禁部署。

**第③类 · 存疑**:
当前无法验证的反馈。review 事实记未确定,仍须评估是否存在具体重大风险;不能整类当普通参考或整类暂停。

**问题记录(FINDINGS.md)**:
review 的裁定权威。稳定 F 编号关联 D 决策,记录原始来源、发生条件、取证分类、事实、证据/缺口、影响、裁定、范围、解除条件、决策冲突、生命周期和历史。同一故障归并但保留全部来源;跨轮保留,已解除问题重开须新证据或具体回归。
_Avoid_: 每轮重新编号的意见清单、仅保留本轮新增发现

**事实结论**:
待验证 / 证实 / 证伪 / 未确定。交付前必须处理完待验证项;查无实据、无定论与③均为未确定,不等于证伪或通过。
_Avoid_: 属实即必改、未验证即不存在

**阻断项(blocker)**:
有证据支持、影响核心目标或验收、安全、数据正确性或实施前置条件,因此必须先解除才能推进相关工作的缺陷。属实本身不足以构成阻断。
_Avoid_: 全部已证实问题、旧「必改项」的直接同义词

**重大风险待决**:
有具体严重后果场景,但缺少关键证据或用户决策而不能排除的风险。它暂停相关工作,但不被表述为已证实缺陷。泛泛的“最好更健壮”不足以进入此类。
_Avoid_: 已确认故障、所有未确定反馈、无条件放行的普通关注项

**非阻断建议**:
不影响相关工作安全、正确地推进的改进参考。交付可精选收益明确且范围相关的建议,无需凑数;未获用户明确采纳前不进入规格/票。
_Avoid_: 自动待办、隐含新增需求

**活动约束**:
生命周期为开放、裁定为阻断或重大风险待决的 F 记录。用户先交付、夜间预算耗尽、无下游依赖或“停靠”均不解除它。
_Avoid_: 全部开放问题(非阻断建议也可以开放)

**结论区(verdict block)**:
SUMMARY.md 从已裁定的 FINDINGS 机械投影五字段:状态、活动约束详情、各家裁决、信号、统计。裁定先写问题记录,交付不另造判断。

**状态(status)**:
单个评审环的推进判定:可推进(活动约束空)/ 相关路径暂停(仍有活动约束,列数量与范围)。与管整条链的「终态」不同。
_Avoid_: 与终态混用、结束即通过

**相关路径暂停**:
仅暂停受未解除问题影响的工作及其依赖方,允许确定独立的工作继续。暂停不等于完成,不能凭未知的依赖关系假定其他工作独立。
_Avoid_: 无条件跳过、所有风险都整链停止

**评审附录(review appendix)**:
review达到结论性终态的交付:活动约束及证据/缺口、影响、暂停范围和解除条件,已解除摘要,精选非阻断参考。内容来自D/F记录,一般建议不是新增需求。完整night只写 `.xcheck/<ts>/review-appendix.md`,不追加被评原文件;正常审核/auto-review的文件型对象仍追加文末(按ts幂等重写),原稿正文不动。
_Avoid_: 全量意见转需求、所有③命中都停整链

### 自动链

**自动链**:
一次 `/xcheck` 触发后的自动推进路径:摄入 → 盲评 → 三分类 → 查证① → 实验② → 裁定交付。review正常交互到结果停点再由用户决定是否修订,无人值守按有限策略处理;diag止于汇总与三分类。
_Avoid_: 全流程(分不清是否含下游实施)

**入口一问(entry choice)**:
壳在新链且无模式旗标时的一次三选:**完整night(默认推荐)/ 仅无人值守审核 / 正常交互审核**。`--night`和`--auto-review`直接选择对应模式,同时使用报互斥错误,不弹两个串行问题。
_Avoid_: 把完整night理解为只审核

**交互方式与执行终点(interaction / target)**:
所有新PROGRESS保存两个独立维度:interactive/review=正常审核,unattended/review=仅无人值守审核,unattended/implementation=完整night。INTERACTION决定是否问人,TARGET决定是否接第11步;仅review终点不建NIGHT、不通知/推送/PR,但仍允许审核所需本地修订与附录。diag任何入口都不实施;完整night允许NIGHT短稿及可选通知,auto-review diag不建NIGHT、不通知。`run-mode.sh`只规范化并校验MODE_REQUEST及持久化模式,不是执行器或权限授予器。
_Avoid_: NIGHT_MODE同时代表是否问人和是否实施、无人值守等于实施授权

**仅无人值守审核(auto-review)**:
`--auto-review`自动审核、必要时最多修订并限定复审一次,到审核交付结束。遇预算或决策边界可使用兼容终态“夜间收工”,不进入实施或发布。
_Avoid_: 夜链、零本地文件写入模式

**停点(gate)**:
结果阶段等用户拍板的时刻。优先说明阻断/待决及具体决策冲突;选择先交付不解除约束,“修订再评”泛答不授权改变既定产品行为。auto-review与night按有限策略自动处理,不伪记为用户决定。

**进度文件**:
阶段完成即落盘的状态记录,是恢复与排障的权威;schema2 还须核验必要材料及账本,不能仅凭勾选或会话记忆跳阶段。
_Avoid_: 会话状态、勾过就一定可跳

**断点续跑**:
无旗标发现未完成链后确认续跑;显式无人值守旗标仅自动续兼容原链。所有续跑继承账本interaction/target,显式模式不符停止,不改账接管。仅两新字段同时缺失才按旧night=on映射完整night、无night映射正常审核;半缺、非法值、与night标记冲突拒绝。schema2从第一个未勾阶段整段重做,沿next/prev定位最新环。旧未完 review 保留原目录,幂等建立 migrated_from 新环重新审核;旧night还须独立检查delivery_schema与冻结元数据,即使review_schema=2也不能补猜基线;缺失则停止自动恢复并说明需明确新开/专门恢复,不重放旧发布动作。

**自动修订预算(auto_revisions_used)**:
原生新review首环为0,每次自动修订递增并跨环保留。旧链迁移虽然新环round=0,不能重置预算:旧round≥1至少记1,无法核实写unknown并禁止自动再修。
_Avoid_: 迁移等于获得新一次自动修订许可

**终态(terminal state)**:
七种结束标签:收敛 / 推倒重来 / 无需修订 / 用户不修 / 夜间收工 / 用户中止 / 完成(diag)。schema2 无需修订 = round 0 活动约束空;收敛 = 修订后活动约束空;用户不修 = 用户先交付但保留约束;夜间收工 = 策略到达预算/决策边界后交付;推倒重来仅用户明确放弃。终态是审核段结束,不代表实施完成。

**夜链(night chain)**:
入口选“完整night”或 `--night` 触发的无人值守实施路径(unattended/implementation)。仅对已确认范围内可修复的阻断最多自动修订一次并限定复审;仅重大待决、需变更已定决策、无可执行修复或一轮后仍有约束则交付夜间收工。随后内联 to-spec → to-tickets → worktree 逐票 TDD 与独立双轴评审,仅推进确定独立的工作。

0.21.0按delivery_schema=1交付:审核共识/修订/附录只写原仓.xcheck,source=inline、original_source仅追溯;spec/票生成前建仓库外worktree,文档/必要.gitignore/代码全部同一夜链分支提交。原分支不commit/push/pull/rebase,不自动stash或复制未提交业务变更。发布只推冻结目标夜链分支,PR显式repo/base/head且自包含,不为PR推base,绝不force/自动合并。无remote可本地执行,无Git/HEAD只审核并记不能实施原因。
_Avoid_: 全自动模式(与「自动链」混淆)、worktree等于安全沙箱、全绿等于已发布

**交付协议与冻结基线(delivery_schema = 1)**:
与review_schema独立的完整night交付版本。首次摄入冻结host_repo、完整start_oid、source_branch、remote_name、唯一push remote_url、pr_base,复审继承不重算。原始对象仅由original_source追溯,不能成为写回目标。旧元数据缺失不能用当前HEAD补猜启动时点;含凭据URL不写账本、禁止自动发布。
_Avoid_: review_schema=2已经保证交付兼容、执行时当前HEAD就是启动基线

**同分支交付与可核实恢复**:
`night-git.sh prepare/verify/publish`分别准备仓库外worktree、核验身份/分支/提交可达、只发布冻结夜链分支。spec/票之前落NIGHT的branch/worktree意图;已有产物恢复先verify并核实所有complete完整OID和测试/独立评审证据。worktree/分支/提交丢失本批暂停,不自动重建再跳票。Git可达不代表测试通过,PR不创建也不抹掉本地实施成果。
_Avoid_: 短SHA或“绿”即可证明完成、丢失时从spec重建继续旧账本

**NIGHT.md**:
夜链下游账本,四阶段 review / plan / impl / finish,review夜链写review_schema=2、delivery_schema=1及继承的冻结元数据、branch/worktree。票级台账区分 complete(完整可达OID及测试/独立评审证据)、paused(直接约束及解除条件)、blocked(依赖未完成)。只有依赖已验证 complete 才放行。有paused/blocked或全局待决时写 waiting(解除条件与证据基线),impl/finish保持未勾;晨报可先交付,链仍等待解除。续跑先核新证据/决策并更新D/F再重算,没有新证据就返回暂停摘要,不重复实施、发布或通知。全部票处理完成才勾finish;全局受约束时plan也未勾,不拆可执行票、不建实施分支、不推送。
_Avoid_: 夜链 PROGRESS、台账有行即完成

**夜间收工(night-shipped)**:
无人值守策略到达修订预算或决策边界的兼容审核终态,auto-review和night均可使用。活动约束继续存在;target=review交付即止,只有完整night下游可做明确独立的工作。
_Avoid_: 用户不修(人不在场)、全面放行、实施权限

**夜链结论(night verdict)**:
收尾对话/通知/PR/晨报共用:全绿(全部票已验证 complete 且无活动约束/终局阻断)/ 带停靠完成(全部票 complete,仅留非阻断参考)/ 未完成(有 paused/blocked 或活动约束,列解除条件)/ 失败收工(spec/拆票失败)。短稿记“未执行实施(<原因>)”。
_Avoid_: 与评审段「终态」混用、局部可推进等于整体完成

**停靠(docked)**:
先不修并留账的展示用语,不是票完成状态。新版只可把非阻断参考作为“带停靠完成”的残留;真实阻断须记 paused 并传播依赖,不能靠停靠解除。
_Avoid_: 跳过即完成、真实但无依赖就当完成

**晨报(MORNING.md)**:
夜链收尾的人话交付:评了什么/改了什么/执行了什么/早上要决定什么,加裁定附录、票状态、解除条件、分支/发布结果和产物路径。对话简报是它的浓缩投影,不是另一份文件。
_Avoid_: 夜链 SUMMARY、暂停未解却只建议重跑

### 流程各步

**摄入(intake)**:
固定用户指定对象及讨论共识。明确文件/贴文按显式对象,明确“刚讨论的方案”优先当前讨论;近期文件、CONTEXT/ADR不抢对象。只对对象或重要决策歧义正常模式确认,无人值守留待决与范围,全局目标不明则停止。
_Avoid_: 按最新文件替用户选对象、默认助手提议已获确认

**冒烟(smoke)**:
fan-out 前对每家 agent 的活性预检(读小文件并回显);预算按 agents.toml 的 smoke_timeout_sec(缺省60),超时只自动重试一次。它验证活性和读材料能力,不证明权限隔离。
_Avoid_: 安全沙箱验证

**选集(SELECTED)**:
实际参评名单,来源优先级 --agents > default_agents > 报错要求设默认集;取已装交集后再冒烟筛选。
_Avoid_: 候选集(CANDIDATES 是筛选前名单)

**派发(fan-out)**:
并行向各家发评审任务。首轮互不可见;复审共同读取上轮问题基线,仍不可见本轮他家输出。

**搬运工(carrier)**:
包装外部 CLI 的 subagent,仅调脚本、等待、读文件、忠实搬运。缺字段如实写未提供,不补推理。
_Avoid_: 评审员、裁定者

**汇总(synthesis)**:
主会话综合结构化结论;review 先出紧凑头,最终裁定写 FINDINGS 再投影;diag 沿用共识/分歧/存疑诊断长文。

**查证(verification)**:
对①类及旧问题解除条件只读核实,将证实/证伪/未确定及证据写回 F 记录。
_Avoid_: 复审、事实成立自动升级阻断

**实验(experiment)**:
对②类本地沙箱验证,临时文件只落 `.xcheck/<ts>/exp/`,禁改业务代码、禁联网、禁部署。成立/不成立/无定论分别映射证实/证伪/未确定。

**修订(revision)**:
针对已授权且可修复阻断,基于本轮快照写方案新版本 `<原名>.rev<m>.md`。完整night只在.xcheck写新稿,原文件由original_source追溯;正常审核/auto-review文件型仍写原文件同目录。原稿正文不动;一般建议不自动加入,未授权的决策冲突不能擅改。
_Avoid_: 把全部建议润色进新稿

**限定范围复审(re-review)**:
验证原活动约束的修复、修改引入的回归和新发现的重大缺陷。re-review-context.md 带上轮完整 F、D 约束、差异和逐条修复理由。普通新增建议不触发再修;原问题没再提到不等于解除。冒烟满足最近真实成功、配置指纹一致、上一环完整成功三条件才可跳;collect 判活仍兜底。
_Avoid_: 每轮重新开题、完全不允许新增发现

**收敛(converged)**:
修订后当前审核范围内没有未解除的阻断或重大风险待决。无需建议清零,不代表实现或测试已通过。正常模式两轮后仍可由用户决定再修/交付/放弃,无人值守审核与night最多一次自动修订。
_Avoid_: 零问题、无条件完美

**推倒重来(start over)**:
用户明确选择放弃方案的终态;不能因细节未清零、自动预算耗尽而替用户判推倒。

### 基础约束与旧协议

**异构(heterogeneity)**:
希望取得不同模型来源的第二意见,而不只是不同 CLI 名字。当前选集全 claude 只标注不拦;不同 CLI 也可能接同一模型,来源未知不得冒称已验证异构。

**盲评(blind)**:
独立评审进程,本轮他家结论不可见;不是操作系统权限隔离。

**材料评审(当前限制)**:
当前提示词要求评审员只读提供的材料,仓库事实由主会话补充查证。CLI 的实际权限尚未实现统一强制隔离,不能称全库取证或可靠沙箱。
_Avoid_: 提示词等于权限、cwd/worktree等于隔离

**旧版已验证三分类清单 / 必改项**:
仅用于无 review_schema 的历史 review:①证实 + ②成立的机械并集为必改项,①②使用 #n、③无编号;旧收敛表示该并集清零。旧记录只读保留,不能用新版阻断语义重解释。diag 保留旧三分类与完成(diag),不做验证链、不写新版 D/F 账本。

### 后续目标(尚未实施)

**可分发配置 / 强制材料隔离**:
分别为第四、五批目标:分发模板与个人配置分离、统一解析;逐CLI验证真正只读/材料访问边界。当前未实现,不得用这些目标描述现有安装或安全保障。第三批同分支交付已纳入delivery_schema=1,但worktree不是权限隔离。
