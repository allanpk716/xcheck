# xcheck 0.22 精简管道重构 · 实施计划

日期：2026-09-19 · 设计：[2026-09-19-xcheck-0.22-lean-pipeline-redesign.md](../specs/2026-09-19-xcheck-0.22-lean-pipeline-redesign.md) · ADR：0004–0007
分支：`feat/lean-pipeline`（自 `origin/feat/review-convergence` 切出）· 开发不强制并行。
每批次一个 commit，独立可验；**动手前先把不变式清单（设计文档 19 条）贴在手边，批次 B/C 的每次改写逐条自检**。

## 批次 A · 机械层收缩

| # | 任务 | 文件 |
|---|---|---|
| A1 | `night-git.sh` 重写：`start <repo> <branch> <start_oid>`（分支不存在则 `git switch -c`，存在则确认当前就在该分支）；`publish <repo> <branch> <remote_url>`（显式 URL 直推 + `refs/heads/<branch>:refs/heads/<branch>` + `--no-verify` + `GIT_TERMINAL_PROMPT=0` + `-c credential.helper=` + 无 force）。保留环境守卫现有 7 变量即可，不加新的（TM-3 出范围） | `xcheck/lib/night-git.sh` |
| A2 | 删除 `night-pr.sh` 及 `night-pr.test.sh` | `xcheck/lib/`、`xcheck/tests/` |
| A3 | 测试收缩：night-git.test.sh 重写为不变式断言（原分支 ref/tag 全程不被推、无 force、hook 借 publish 偷运被 `--no-verify` 阻断、凭据缺失快速失败不挂起、start 不动原分支未提交内容） | `xcheck/tests/night-git.test.sh` |
| A4 | flow.md 第 11 步接线改口径：删 worktree/prepare/verify 叙述，改"start 接管检出 + 每票提交后 publish"；晨报/通知加 compare URL 拼接（`<remote_url 去掉 .git>/compare/<pr_base>...<branch>`，Gitea 同式） | `xcheck/lib/flow.md`、`night-delivery.md` |

验证：`bash xcheck/tests/night-git.test.sh` 绿；`bash -n` 两脚本；review-contract 套件复跑绿（接线 grep 会指向 A4 已改的措辞，同步更新）。

## 批次 B · 账本塌缩（受不变式清单约束）

| # | 任务 | 文件 |
|---|---|---|
| B1 | PROGRESS/NIGHT 模板改：删 `delivery_schema`+七冻结字段族+`night=on` 写入+`implementation_blocked/publication_blocked` 字段族；NIGHT 记 `start_oid/branch/remote_url/pr_base` 四字段；无 git 情形一句话记录（"无 git，仅审核"），不起字段族 | `flow.md` |
| B2 | 删 `NIGHT_MODE`：SKILL/flow 所有派生与清除规则删除，闸门只看 TARGET | `xcheck/SKILL.md`、`flow.md` |
| B3 | 旧链一律拒绝：删迁移矩阵、`migrated_from`、`auto_revisions_used` 迁移核算；扫描遇无 schema 旧链 → 一句"旧协议链已不支持，请新开" | `flow.md`、`review-contract.md` |
| B4 | **字段字典单源节**：flow.md 文末新增一节，唯一定义每个 PROGRESS/NIGHT 字段（名/枚举/写入时机/继承）；AGENTS/README/CONTEXT/artifacts 改为引用不重抄 | 五文档 |
| B5 | grep 同步锁测试：字段名在字典外文档中不出现未定义变体（锁 `implementation_blocked` 类事故：字典外不得出现字典字段名的近义变体）；票 complete 行格式全仓唯一 | `xcheck/tests/review-contract.test.sh` 增段 |
| B6 | 已漂移规则去重为单一权威句 + 引用：重开规则（4 处→1）、diag 不实施（3 处→1）、终点守卫（3 层→1+引用） | `review-contract.md`、`SKILL.md`、`flow.md` |

验证：grep 同步锁绿；review-contract/run-mode 套件复跑绿；字段数字典内外一致（人工抽查 5 字段）。

## 批次 C · 丢失保证回填

| # | 任务 |
|---|---|
| C1 | 复审环钉死面板：flow 10.3 继承清单加 `selected=<旧环 selected ∩ 当前 INSTALLED>`；已卸载用交集+注明不弹窗 |
| C2 | 终审最强档：11 步终局全分支 review 恢复"派最强档 reviewer" |
| C3 | 铁律 3 改齐："第 9 步停点一问三处"→"第 9 步停点（未决逐个问）三处"；gate 注释选项名对齐 9.4 现行选项 |
| C4 | 修订稿自代入纪律回填（10.1）；第 7 步查证边界回填（只读判据直指处）；边界表"夜间解析不出对象"行限定 TARGET=implementation；0.6 背景声明恢复"未经逐条确认，若有出入请打断"；"能核实帮助"残句改为"能核实 tea 可用且目标三元组显式"；deliverable 恢复正向勾选指令 |

验证：**不变式清单 19 条逐条核对表**（每条：main 上行为出处 → 0.22 上行为出处，成对填表入 PR 正文）。

## 批次 D · 并行实施

| # | 任务 | 文件 |
|---|---|---|
| D1 | to-tickets 产出增"涉及路径"字段（票模板 + 契约一句话：列该票预期改动的文件/目录） | `flow.md` 11 步 |
| D2 | 调度协议：事件驱动就绪集（票落地→重算就绪集→补位派出；就绪 = 依赖满足 ∧ 路径与在跑票不相交 ∧ 泳道有空位）；逐票评审随票出发；恢复 = 从票账本重算就绪集 | `flow.md` 11 步 |
| D3 | 提交纪律：泳道 agent 只改文件+局部验证不 commit；主会话逐票验收→按票 commit（显式路径）→publish；落者超时/失败记 paused 不拖队 | `flow.md`、`subagent-carrier.md` |
| D4 | 派单包：票描述+路径标注文件内联（预算上限，超限关键文件全文+其余大纲）+验收标准；模板前缀稳定 | `subagent-carrier.md` |
| D5 | 票级评审只喂票 diff+票描述；`night_parallel_lanes`（agents.toml，默认 2） | `flow.md`、`agents.toml` |
| D6 | 调度契约测试：依赖阻塞/路径重叠不并行/提交串行/落者 paused/恢复重算就绪集 的静态断言 | `xcheck/tests/` 新套件 |

验证：新套件绿 + **live 夜链**（小任务、2 泳道，跑四指标）。

## 批次 E · 收尾

E1 子代理 CLI 轮询改后台任务+回调（约省 20 分钟/夜）；E2 文档：夜链建议 0 点后启动、README 记 bash≥4.3/git 下限、CONTEXT.md 补术语（泳道/就绪集/接管检出）；E3 CHANGELOG 0.22.0；E4 live 验证执行并记录四指标入 CHANGELOG。

## 里程碑判据

- M1（A+B+C 完成）：全部离线套件绿 + 不变式核对表填满 → 可合 main 的第一个检查点
- M2（D 完成）：调度契约测试绿 → 预合并
- M3（E 完成）：live 四指标达标 → 打 `v0.22.0` 合 main
